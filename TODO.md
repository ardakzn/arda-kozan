# TODO

## Final Polish

- Fix the fallback PDF loading text in `src/pages/Home.tsx` (`Loading PDFÔÇĞ`).
- Fix the mojibake image caption in `public/data/projects.json` (`selamlÄ±yor`).
- Review remaining hardcoded English UI labels and decide whether they should be localized.
- Re-run lint/build/typecheck after polish changes.
